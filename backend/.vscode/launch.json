{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Backend: Actix (debug)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/target/debug/backend",
      "args": [],
      "preLaunchTask": "backend: build (debug)",
      "envFile": "${workspaceFolder}/.env.dev",
    }
  ]
}
